# SyncronizationAppClient
Команда запуска клиента:
```
./main [address] ws
```
В данный момент сервер адаптирован под локальный сервер, так что доступ осуществляется по адресу: ```localhost:8080``` или же локальный адресс устройства
